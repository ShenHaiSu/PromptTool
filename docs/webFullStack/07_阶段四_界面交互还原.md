# 07 阶段四 — 界面交互还原（像素级对齐 tauri）

## 范围

`App.vue/DimensionPanel/BatchFactory/BatchCard/HistoryPanel/StatusBar` + 全部 Dialog + composables，不动视觉只换数据源。

## 步骤

1. `App.vue`：保留三栏 `30:38:32` + Sash `pointer capture + rAF` + `contain: layout paint` + `data-testid`；删除 DB onboarding，`onMounted` 只做 `library.fetchAll + history.fetchAll + temp_carry（kv）恢复`。
2. 维度面板：搜索/禁用/折叠/右键（批量新增/翻译/生成）原样保留；翻译/生成/批量对话框逻辑不动（解析器已是纯前端）。
3. 批量工厂：`TanStack Virtual` 参数不动（`h-[400px]/estimateSize 110/overscan 5`），复制/导出走前端 `exportBatchCsv`。
4. 历史/模板/收藏：快照读写走 IDB；“已失效”占位与回填二次确认保留。
5. 全局能力：`useShortcuts`（Ctrl+F/S/C + Delete，输入框不劫持）、主题（`pmf:theme` + StatusBar 切换 + 首屏防闪）、Sash/几何（`pmf:sash/pmf-geometry` + beforeunload）、Toast（MAX 5）、`app.config.errorHandler` 原样保留。
6. `tokens.css` 深浅色、Dialog `Teleport+z70+calcMenuPos`、禁止硬编码色，三项设计铁律逐文件 `grep` 验收。

## 验收

- 与 tauri 版同数据下截图diff：三栏比例、对话框位置、暗色主题一致；`Layout.test.ts` 等 UI 单测全绿。
