# Need05 — `92cabce` 之后功能性提交剥离与 Tauri 落地指南 · 总览

> 基准：`92cabce feat(web-pure): 阶段六构建部署与验收` 之后 → `HEAD(web-pure 7f91c49)` 共 **9 个提交**。
> 结论：**功能性 2 个**（`9febdf6` + `efb71fc`，即 need04 的施工落地），其余 7 个为 chore/refactor/docs/merge，Tauri 分支**无需**移植。

- 输出路径：`docs/need05/`
- 版本：v1.0 — 2026-09-21
- 读者：切到 `tauri` 分支做功能对齐的人（即后续的你）
- 细则文档：
  - [01_维度排序真相源与随机链路.md](01_维度排序真相源与随机链路.md) — `9febdf6 feat(need04)` 全量解析
  - [02_新建导入sortOrder自增.md](02_新建导入sortOrder自增.md) — `efb71fc fix(need04)` 全量解析
- 上游设计：`docs/need04/00-07`（本需求就是 need04 设计的施工实现，设计本身不用重写，Tauri 侧直接复用）

---

## 一、9 个提交一览（按时间先后）

| # | 提交 | 类型 | 是否功能性 | 一句话 |
|---|------|------|-----------|--------|
| 1 | `0f775c7` | chore | ❌ | 删除 `src-tauri/`、`.bak`、Tauri 文档与 `import_sample_prompts.py`（纯 Web 清理） |
| 2 | `f45d412` | chore | ❌ | `package.json` 改名、标题去 Tauri 后缀、`.vscode`/`.gitignore`/`vite.config` 去 Tauri 化 |
| 3 | `991ff3f` | refactor | ❌ | 删 11 个测试文件的 `@tauri-apps/api/core` dead mock；README/CHANGELOG 转纯 Web 口径 |
| 4 | `8e65092` | docs | ❌ | README 纯 Web 口径补全（59+/18-） |
| 5 | `651eb71` | docs | ❌（设计文档） | 新增 `docs/need04/00-07`：随机生成尊重维度顺序权重的设计方案（Tauri 侧**可直接读**，不用移植） |
| 6 | **`9febdf6`** | **feat** | **✅ 功能 A** | **维度排序以 DB `sortOrder` 为唯一真相源**（`buildDimOrderMap` + 三级回退 + 随机/画布双链路透传）→ 详见 01 |
| 7 | **`efb71fc`** | **fix** | **✅ 功能 B** | **新建与导入维度 `sortOrder` 按 `max+1` 自增**（不再固定 `0`）→ 详见 02 |
| 8 | `9b53b24` | merge | ❌ | `web-pure` 自合并，无内容 |
| 9 | `7f91c49` | chore | ❌ | `.gitignore` 加 Tauri 构建残留忽略，无功能 |

验证命令：

```powershell
git log --oneline 92cabce..HEAD --reverse
git show 9febdf6 --stat
git show efb71fc --stat
```

---

## 二、为什么只有 2 个算功能性

- `0f775c7 / f45d412 / 7f91c49`：纯删除/改名/忽略文件，不改变任何运行时语义。Tauri 分支**反向操作都不需要**（Tauri 本来就要留着 `src-tauri/`）。
- `991ff3f`：唯一代码改动是删测试里的 dead `vi.mock('@tauri-apps/api/core')` 行 + 文案注释把“Tauri 版”改成“旧版”。经 `git show 991ff3f` 确认，无逻辑变更。Tauri 分支应**保留** mock，不要移植。
- `651eb71 / 8e65092`：只加 `docs/` 与 `README.md`，零 `src/` 改动。
- `9b53b24`：空合并。

## 三、Tauri 落地总表（先看结论，再看细则）

两个功能在 `tauri` 分支的状态（已用 `git show tauri:<path>` 核实）：

| 功能 | `web-pure` 改了哪里 | `tauri` 现状 | 落地动作 |
|------|---------------------|--------------|----------|
| A 排序真相源 | `src/engine/assembly.ts`、`src/engine/random.ts`、`src/stores/assembly.ts`、`src/stores/library.ts`（纯前端） | **完全缺失**：`assembly.ts` 仍是 `?? 99` 旧口径、无 `buildDimOrderMap`；`random.ts` 的 `assemble(picked, config)` 未传 `map`；两处 store 均无 `dimOrderMap` | 前端 4 文件**可直接照搬**（01 文档给逐文件 diff）；`src-tauri/` **不用动**（排序只发生在 TS 引擎） |
| B 自增 `sortOrder` | `src/lib/db.dimensions.ts`、`src/lib/db.library.ts`（IndexedDB） | **同 bug 存在于 Rust 侧**：`src-tauri/src/commands/db.rs` 中 `db_create_dimension` 用 `sort_order.unwrap_or(0)`，另有 2 处 `.unwrap_or(0)`；前端 `src/lib/db.ts` 是 invoke 封装无自增逻辑 | Rust 侧按 02 文档 §4 重写为 `MAX(sort_order)+1` + 导入 `nextSort` 递增；前端 `db.ts`  **不用动** |

移植顺序建议：**B（数据写对）→ A（读排序用对）**，与 `need04-07` 施工顺序一致。两处单测（`assembly.test.ts`、`random.test.ts`、`db.idb.test.ts`）在 Tauri 分支有同名文件，可把 need04 新增用例复制过去验证。

## 四、文件清单

```
docs/need05/
  00_总览.md                        ← 本文件
  01_维度排序真相源与随机链路.md     ← feat 9febdf6
  02_新建导入sortOrder自增.md        ← fix efb71fc
```
