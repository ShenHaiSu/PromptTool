# 06 测试与验收

## 1. 后端单测（`cargo test -p tauri-app --lib path_resolve / migration`）

| # | 用例 | 断言 |
|---|---|---|
| B1 | `strip_verbatim("\\\\?\\D:\\a.db")` | `== "D:\\a.db"` |
| B2 | `strip_verbatim("\\\\?\\UNC\\s\\sh\\a")` | `== "\\\\s\\sh\\a"` |
| B3 | `norm_key("\\\\?\\D:\\A.db")==norm_key("d:/a.db")` | Windows 下相等（Unix 下大小写敏感分支另测） |
| B4 | `canonical_stripped` 对存在文件 | 无 `\\?\` 前缀 + 文件仍可 open |
| B5 | `default_image_dir_for(data, Some(doc), legacy=true)` | 返回老默认 `data/output/images` |
| B6 | `default_image_dir_for(data, Some(doc), legacy=false)` | 返回 `doc/Prompt Modular Factory/images` |
| B7 | `default_*_for(data, None, false)` | 回退 `data/output/...` |
| B8 | 迁移 `migrate_path_schema_v2` fixture（含 `\\?\` + 重复行 + 前台） | `washed=3 deduped=1`，重跑幂等 `washed=0`，`foreground` 可用 |
| B9 | 迁移失败注入（只读 db） | 返回 Err 且原库 unchanged，有 `.bak` |

## 2. 前端单测（`vitest run src/lib/__tests__/pathDisplay.test.ts`）

- `stripVerbatim` 与 B1/B2 同值（前后端锁定，不漂移）。
- `onOpenOutput` 空输入不再 `invoke('db_reveal_in_explorer', {path: '<...>'})`，而是 `invoke('iq_open_output_dir')`（mock invoke 断言）。
- `DbManagerDrawer` 展示 `\\?\D:\x` → 渲染 `D:\x`。

## 3. 集成/回归（不碰现有绿）

- `bunx vitest run` 全绿；`cargo test` 全绿；`bun run build`（vue-tsc 0 error）。
- need05/06 回归：`normalize_image_dir_three_states` 等旧测例保持通过（`normalize_image_dir_for(data,"")` 在 `legacy=false` fixture 下行为变更 → 允许更新断言，但必须在 PR 中注明）。

## 4. 手工矩阵（Windows 真机，关闭门禁）

| 场景 | 操作 | 期望 |
|---|---|---|
| Program Files 非管理员首装 | 双击启动 | 首启成功，`path_get_bases.base=appdata`，无"管理员"弹窗 |
| 老用户升级（含 `\\?\` 存量） | 覆盖安装启动一次 | DB 页无 `\\?\`，前台/驻留正确，`.bak-<ts>` 存在，日志 `washed>0` |
| 默认输出未配置 | 点"打开输出目录" | 打开解析目录（`iq_get_resolved_output_dir` 同值），不再跳 Documents；目录自动创建 |
| 配置绝对目录（含空格） | 输出设 `D:\My Pics\PMF` 后打开 | 正确打开；文件 `/select` 选中态正常 |
| OneDrive Documents | Documents 重定向环境点打开 | 正确跟随重定向，无报错 |
| 补路径/重建/删除 | DbManager 全操作一遍 | 提示路径均为 display 形态；删除确认无 `\\?\` |
| 回滚演练 | 删 Default.db，用 .bak 恢复重启 | 库列表/前台恢复，`path_migrate_status.version` 回 v1 再升 v2 正常 |

关闭标准：上表全过 + §1/§2 全绿 + 无新增 `\\?\` 入库（抽查 `SELECT path FROM db_registry`）。
