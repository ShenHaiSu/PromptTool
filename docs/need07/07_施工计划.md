# 07 施工计划（P0–P3 + 文件清单 + 回滚）

## P0 后端中枢纯函数（不碰行为，约0.5天）

- [ ] T1 新建 `src-tauri/src/commands/path_resolve.rs`：`strip_verbatim/display_path/norm_key/lexical_normalize/canonical_stripped/default_*_for` + 单测 B1–B7（见 `06`）。
- [ ] T2 `commands/mod.rs` 注册模块；`cargo test path_resolve` 绿。
- 验收：`cargo test -p tauri-app --lib path_resolve` 全绿；零业务改动。

## P1 注册表迁移 + reveal/ensure（约1天）

- [ ] T3 `meta.rs` 新增 `migrate_path_schema_v2` + 单测 B8/B9；`lib.rs setup` 在 `init_default_db` 后调用（失败不阻断启动，只日志）。
- [ ] T4 `business.rs normalize + wasForeground` 改 `norm_key`；`export.rs normalize` 同改；`db_get_active_info/db_list_registry` 返回前脱壳。
- [ ] T5 `path_resolve.rs` 薄适配层：`resolve_data_dir/resolve_image_dir/resolve_export_dir/ensure_dir`；`lib.rs setup` 改用 `active`；`queue.rs check_disk_space` 脱壳。
- [ ] T6 重写 `db_reveal_in_explorer(app, path)`（双参数 explorer + ensure + opener 回退）+ 新增 `iq_open_output_dir/iq_get_resolved_output_dir/path_get_bases`；`lib.rs` 注册 4 命令。
- [ ] T7 备份回滚演练一次（见 `04`-§4）。
- 验收：`cargo test` 全绿；`SELECT path` 无 `\\?\`；`06`-§4 中 Program Files/升级两行通过。

## P2 前端契约 + 视图（约0.5天）

- [ ] T8 新建 `src/lib/pathDisplay.ts` + `src/lib/__tests__/pathDisplay.test.ts`（与 B1/B2 锁定）。
- [ ] T9 `ImageQueuePanel.onOpenOutput` 改调 `iq_open_output_dir`（删占位符）；`imageQueueApi.ts` 加 `iqOpenOutputDir/iqGetResolvedOutputDir/pathGetBases`；`viewToConfig` 洗旧缓存。
- [ ] T10 `DbManagerDrawer`（§97/108/42）+ `LibraryDialog` 展示过 `displayPath`；`ImageQueueSettings` placeholder/保留 hint。
- [ ] T11 更新三处 `__tests__` mock（`dbRevealInExplorer` 保持，新增 `iq_open` mock）。
- 验收：`bunx vitest run` + `bun run build` 绿；`06`-§4 全矩阵通过。

## P3 打磨关闭（约0.5天）

- [ ] T12 删 `docs/need05/apikey.txt`（如仍在），`git status` 干净；补 `CHANGELOG` 一行。
- [ ] T13 收敛三处 `lexical_normalize` 重复实现为中枢调用（business/export/queue 各留一行转发，避免行为漂移）。
- [ ] T14 文档回写：`pitfalls/02-windows-path.md`（canonicalize/reveal/比较三坑）。

## 文件清单（预计）

```
新增：
  src-tauri/src/commands/path_resolve.rs
  src/lib/pathDisplay.ts / src/lib/__tests__/pathDisplay.test.ts
  docs/need07/00..07（本系列）
  docs/pitfalls/02-windows-path.md
修改：
  src-tauri/src/commands/{mod.rs,meta.rs,business.rs,export.rs,migration.rs}
  src-tauri/src/commands/image_queue/{queue.rs,config.rs,mod.rs}
  src-tauri/src/lib.rs
  src/{lib/db.ts,lib/imageQueueApi.ts,components/ImageQueuePanel.vue,
       components/ImageQueueSettings.vue,components/DbManagerDrawer.vue,
       components/LibraryDialog.vue}
不碰：
  *.db 表结构（仅 app_settings 加版本键）、图片/业务库文件本体、Cargo.toml 依赖
```

## 风险与回滚

| 风险 | 缓解 |
|---|---|
| 去重误删库 | 按 `norm_key` 分组只删同文件多余行 + 事务 + `.bak`；验收抽查行数 |
| Documents 不可用 | `document_dir()==None` 回退 data；探针标明分支 |
| 旧测例 `normalize_image_dir("")` 断言老默认 | 允许更新断言并在 PR 注明（legacy=false 新语义） |
| 启动迁移失败 | 不阻断启动 + 日志给 bak 路径；手工回滚见 `04`-§4 |

分支建议：`feat/need07-winpath` → 自测 `06` 全过 → squash → `tauri`。
